% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_blocks.R
\name{spatial_block_folds}
\alias{spatial_block_folds}
\title{Spatial Block Cross-Validation}
\usage{
spatial_block_folds(
  data,
  x = NULL,
  y = NULL,
  k = 5,
  block_size = NULL,
  n_blocks = NULL,
  assignment = "systematic",
  seed = NULL
)
}
\arguments{
\item{data}{Spatial observations (data.frame or sf object)}

\item{x}{Name of the x coordinate column (required for data.frame, ignored for sf)}

\item{y}{Name of the y coordinate column (required for data.frame, ignored for sf)}

\item{k}{Number of folds (default: 5)}

\item{block_size}{Size of blocks as c(width, height) in coordinate units}

\item{n_blocks}{Number of blocks in x and y directions as c(nx, ny)}

\item{assignment}{Strategy for assigning blocks to folds: "systematic" or "random"}

\item{seed}{Random seed for reproducibility (default: NULL)}
}
\value{
An object of class "spatial_folds" containing fold assignments
}
\description{
Creates spatial cross-validation folds by dividing the study area into
rectangular blocks and assigning observations to folds based on their
block membership. This method ensures spatial separation between training
and test sets.
}
\details{
The spatial block method divides the spatial extent into a grid of rectangular
blocks. Two approaches are available:

\itemize{
\item Fixed block size: Specify block_size to control block dimensions
\item Fixed number of blocks: Specify n_blocks to control grid resolution
}

If neither is specified, the function attempts to create approximately sqrt(k)
blocks in each direction.

Assignment strategies:
\itemize{
\item "systematic": Assigns blocks to folds in a systematic pattern
\item "random": Randomly assigns blocks to folds
}
}
\examples{
\dontrun{
data(sample_spatial_data)
folds <- spatial_block_folds(
  data = sample_spatial_data,
  x = "longitude",
  y = "latitude",
  k = 5,
  block_size = c(200, 200)
)
}

}
\seealso{
Other spatial cross-validation functions:
\code{\link[=spatial_buffer_folds]{spatial_buffer_folds()}},
\code{\link[=spatial_cluster_folds]{spatial_cluster_folds()}},
\code{\link[=spatial_folds]{spatial_folds()}},
\code{\link[=spatial_split]{spatial_split()}}
}
\concept{spatial cross-validation functions}
